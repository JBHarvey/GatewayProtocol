namespace GatewayProtocol.Payload.Gateway;

enum ErrorCode : uint32 { NoError }

table Error {
  code:ErrorCode = NoError;
  has_recovered:bool = true;
  occurrence_timestamp:string;
  source:string;
  description:string;
}
