include "sensor_information.fbs";
include "sensor_message.fbs";
include "sensor_raw_data.fbs";
include "request.fbs";

namespace GatewayProtocol.Payload.Sensor;

struct Header {
  identifier:string;
}

union Content { Information, Message, RawData, Request }

table Payload{
  sensor_header:Header;
  data:Content;
}
